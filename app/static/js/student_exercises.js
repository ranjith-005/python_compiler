(async function(){const l=document.getElementById('exercise-list');const d=await (await fetch('/api/dashboard/student')).json();l.innerHTML=(d.assignments||[]).map(a=>`<div class="row"><div><div class="title">${a.title}</div><div class="meta">${a.preview||''}</div></div><button class="cb-btn primary" data-id="${a.id}">${a.notebook_id?'Continue':'Open'}</button></div>`).join('')||'<p class="empty-note">No exercises assigned yet.</p>';l.querySelectorAll('button[data-id]').forEach(b=>b.onclick=async()=>{const x=await (await fetch('/api/assignments/'+b.dataset.id+'/open',{method:'POST'})).json();location='/nb/'+x.notebook_id});})()
